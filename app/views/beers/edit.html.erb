<h1>Edit "<%= @beer.name %>"</h1>
<%= render partial: "errors", locals: {object: @beer} %>
<br>
<div class="content">
    <%= form_with(model: @beer) do |f| %>
        <%= render partial: "form", locals: {form_builder: f} %> 
        
        <br><%= render partial: "reviews/submit_button", locals: {form_builder: f} %>
    <% end %>
</div>
<br>
<div class="bottom-nav">
    <a class="bottom-nav-link <%= active_css(beer_path(@beer)) %>" href="/beers/<%= @beer.id %>">Nevermind!</a>
</div>