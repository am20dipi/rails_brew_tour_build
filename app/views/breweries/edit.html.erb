<h1>Edit <%= @brewery.name %></h1>

<%= render partial: "beers/errors", locals: {object: @brewery} %>

<br>
<div class="content">
    <%= form_with(model: @brewery) do |f| %>
        <%= render partial: "form", locals: {form_builder: f} %> 
        
        <br><%= render partial: "reviews/submit_button", locals: {form_builder: f} %>
    <% end %>
</div><br>

<div class="bottom-nav">
    <a class="bottom-nav-link <%= active_css(brewery_path(@brewery)) %>" href="/breweries/<%= @brewery.id %>">Nevermind!</a>
</div>